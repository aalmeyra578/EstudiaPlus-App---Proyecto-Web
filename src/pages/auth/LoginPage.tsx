/** Login screen placeholder (prototype: Inicio / login). */
export function LoginPage() {
  return (
    <div className="flex min-h-svh items-center justify-center px-6">
      <h1 className="font-mono text-4xl tracking-tight">Iniciar sesión</h1>
    </div>
  )
}
